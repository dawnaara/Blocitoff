<%= form_for (@item) do |f| %>

	<%= f.label :title %>
	<%= f.text_field :title, class: 'form-control', placeholder: "Enter new item" %>
	<%= f.submit "Save Item", class: 'btn btn-success' %>

<% end %>